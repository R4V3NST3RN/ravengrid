{
  "name": "ravenstern-css",
  "version": "1.0.0",
  "description": "Osobní CSS knihovna — design tokeny, reset, grid, container queries, komponenty.",
  "private": true,
  "scripts": {
    "build": "NODE_ENV=production postcss css/index.css -o dist/style.css --map",
    "watch": "postcss css/index.css -o dist/style.css --watch",
    "dev": "postcss css/index.css -o dist/style.css --watch",
    "build:clean": "rm -rf dist && mkdir -p dist && NODE_ENV=production postcss css/index.css -o dist/style.css --map"
  },
  "devDependencies": {
    "postcss": "^8.4.38",
    "postcss-cli": "^11.0.0",
    "postcss-import": "^16.1.0",
    "cssnano": "^7.0.1"
  }
}
